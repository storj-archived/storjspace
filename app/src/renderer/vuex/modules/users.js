// import * as types from '../mutation-types'

const state = {
  id: '',
  email: '',
  authed: false,
  privkey: '',
  pubkey: ''
}

const mutations = {
  // [types.LOGIN_USER]
}

export default {
  state,
  mutations
}
